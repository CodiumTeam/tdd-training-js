let assert = require('assert');
let UserRegistration = require('../src/user_registration').UserRegistration;

describe('UserRegistration', function() {
  it('change_this_name', function() {
    let userRegistration = new UserRegistration();

  });
});
